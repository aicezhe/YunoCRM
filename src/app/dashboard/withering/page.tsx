import { TrendingDown } from "lucide-react";
import { ComingSoon } from "../coming-soon";

export default function WitheringPage() {
  return (
    <ComingSoon
      title="Withering"
      description="Open prospects that have gone quiet and need a follow-up."
      icon={TrendingDown}
    />
  );
}
